package entities;

public class SavingsAccountPlus extends SavingsAccount{
	// Final não permite criar a classe com extends de SavingsAccount
	
	/* Final não permite criar método sobreposto da classe SavingsAccount
	@Override
	public void withdraw(double amount) {
		balance -= amount + 2;
	}
	*/
}
